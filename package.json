{
  "name": "splitcam",
  "version": "1.0.0",
  "description": "Picture-in-Picture camera with basic controls",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . splitcam --platform=win32 --arch=x64 --out=dist/ --overwrite"
  },
  "keywords": [
    "camera",
    "pip"
  ],
  "author": "User",
  "license": "ISC",
  "devDependencies": {
    "electron": "^40.6.0",
    "electron-packager": "^17.1.2"
  }
}
